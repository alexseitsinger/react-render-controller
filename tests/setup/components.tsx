import React, { ReactElement } from "react"

export const FirstRender = (): ReactElement => <div>First Render</div>
export const SuccessfulRender = (): ReactElement => <div>Successful Render</div>
export const FailedRender = (): ReactElement => <div>Failed Render</div>
